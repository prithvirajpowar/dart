pipeline {
  agent any
  
  stages {
    stage('Compile hello.dart') {
      steps {
        sh 'dart compile exe hello.dart'
      }
    }
    stage('Compile name.dart') {
      steps {
        sh 'dart compile exe name.dart'
  }
}
