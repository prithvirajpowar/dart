pipeline {
  agent any
  
  stages {
    stage('Checkout') {
      steps {
        checkout scm
      }
    }
    
    stage('Build') {
      steps {
        sh 'dart hello.dart'
      }
    }
    stage('Build') {
      steps {
        sh 'dart name.dart'
      }
    }
  }
}
